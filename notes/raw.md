## Generative AI

User Gives Input ---> LLM Provides Output

## Agent AI

LLM is trained with old data. It does not have access to recent data and thus cannot provide output based on recent data. It has to depend on some third party tool called context. And extracting this data from that third party tool is called Basic Agent.